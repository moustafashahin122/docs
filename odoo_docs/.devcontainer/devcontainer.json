{
  "name": "Python on Ubuntu Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },

  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash",
        "python.defaultInterpreterPath": "/workspaces/odoo_17/.venv/bin/python3"
      },
      "extensions": ["ms-python.python"]
    }
  },
"postCreateCommand": "bash -c 'whoami && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt  && pip install -r ./epm/requirements.txt' ",
  "remoteUser": "vscode"
}
